{
  "name": "@comparator/shared",
  "version": "1.0.0",
  "type": "module",
  "main": "shared.ts",
  "scripts": {
    "check": "tsc",
    "lint": "eslint .",
    "prettier": "prettier --ignore-path ../.gitignore --check .",
    "prettier:fix": "prettier --ignore-path ../.gitignore --write .",
    "test": "echo 'no validator tests'"
  },
  "devDependencies": {
    "vitest": "^4.0.17"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
